/**
 * Initialize authentication on client-side
 */
export default defineNuxtPlugin(async () => {
  // This plugin runs on client-side only after the router is ready
  // It ensures useAuth is available for use in components
})
