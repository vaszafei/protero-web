Place generated JSON files for the Nuxt app under this folder as `/data/<league>.json`.
This folder is populated automatically by `fbref_export/generate_json_data.py` when run.

Run locally:
- Install dependencies: `npm install`
- Dev server: `npm run dev`

The app title is `ΠροΤερο` (Nuxt config).
